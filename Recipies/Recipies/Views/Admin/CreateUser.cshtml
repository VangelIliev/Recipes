@model CreateUserViewModel
<div class="header mt-md-5">
    <div class="header-body">
        <div class="row align-items-center">
            <div class="col">
                <h6 class="header-pretitle">
                    Потребители
                </h6>
                <h1 class="header-title">
                    Създаване на нов потребител
                </h1>
            </div>
        </div>
    </div>
</div>
<div class="card">
    <div class="card-body">
        <form asp-action="CreateUser" asp-controller="Admin">
            <div class="row">
                <div class="form-group col-md-6">
                    <label asp-for="Email">е-Поща</label>
                    <input asp-for="@Model.Email" type="text" class="form-control" required="required">
                    <span asp-validation-for="@Model.Email" class="text-danger"></span>
                </div>
                @if (ViewBag.Roles != null)
                {
                    <div class="form-group col-md-6">
                        <label asp-for="Role">Роля</label>
                        <select asp-for="@Model.Role" asp-items="@(new SelectList(ViewBag.Roles, "Name", "Name"))" class="form-control" placeholder="Роля"></select>
                    </div>
                }

            </div>

            <div class="row">
                <div class="form-group col-md-6">
                    <label asp-for="Password">Парола</label>
                    <input asp-for="@Model.Password" type="password" class="form-control" required="required">
                    <span asp-validation-for="@Model.Password" class="text-danger"></span>
                </div>
                <div class="form-group col-md-6">
                    <label asp-for="ConfirmPassword">Повтори палола</label>
                    <input asp-for="@Model.ConfirmPassword" type="password" class="form-control" required="required">
                    <span asp-validation-for="@Model.ConfirmPassword" class="text-danger"></span>
                </div>
            </div>

            <div class="row">
                <div class="form-group col-md-12">
                    <div class="sign-btn text-center">
                        <input type="submit" class="btn btn-primary btn-block" value="Добавяне на нов потребител">
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="form-group col-md-12">
                    <div class="sign-btn text-center">
                        <a asp-controller="Admin" asp-action="AllUsers" class="btn btn-secondary btn-block">Назад</a>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="form-group col-md-12">
                    <div asp-validation-summary="All" class="text-danger"></div>
                </div>
            </div>
        </form>
    </div>
</div>
